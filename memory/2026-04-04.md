# 2026-04-04 日記

## 19:00 - AutoResearch 隔夜研究

### 任務執行狀態：❌ 失敗（硬體不相容）

**問題：Mac mini M4 無法執行 AutoResearch**
- Mac mini (M4 Apple Silicon) 沒有 NVIDIA CUDA GPU
- torch 2.9.1 是 CPU-only 版本（macOS arm64 無法用 cu128）
- `train.py` 硬編碼 `device = "cuda"` + CUDA Flash Attention kernel
- kernels 庫無 MPS (Apple GPU) 版本

**已嘗試修復：**
1. PYTHONPATH 問題 → 已繞過（直接用 .venv/python）
2. torch CUDA 版本 → 無法在 macOS arm64 上安裝

**建議：**
- 需要 NVIDIA GPU 機器（雲端或實體）
- 或修改 train.py 移除 CUDA 依賴（犧牲速度）

**代價：**
- 本次 apr4 實驗無法進行
