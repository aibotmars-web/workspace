# Isolated Architecture - 自動推進系統

## 概述
OpenClaw Hybrid 模式：Main Agent 對話 + Isolated Sessions 背景執行

## 架構圖
```
┌─────────────────────────────────────────────────────────┐
│                    OpenClaw Gateway                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Main Session (對話)                              │   │
│  │ - 用戶對話入口                                   │   │
│  │ - 規劃方向、查看進度                             │   │
│  │ - 可隨時中斷 (/new)                              │   │
│  └─────────────────────────────────────────────────┘   │
│                           ↓                             │
│                    memory/*.md                          │
│                           ↑                             │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Isolated Sessions (背景自動跑)                   │   │
│  │ - 不受 /new 影響                                 │   │
│  │ - 獨立執行 cron 定期任務                         │   │
│  │ - 每階段寫入 memory                              │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ Assistant│ │ Crawler  │ │Assistant │ │  Trader  │ │
│  │(早晨報告) │ │(知識庫)  │ │-Work     │ │(交易監控)│ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## Cron 任務清單

| Agent | 任務名稱 | Cron 表達式 | 功能 |
|-------|----------|-------------|------|
| **assistant** | 每日早晨報告 | `0 9 * * *` | 天氣、待辦、知識庫檢查 |
| **crawler** | 知識庫自動更新 | `0 */6 * * *` | 爬取 9 個專家頻道 |
| **assistant-work** | 內容發布檢查 | `0 */4 * * *` | X/IG/抖音 發布狀態 |
| **planner** | 專案進度檢查 | `0 20 * * *` | 專案狀態、待辦更新 |
| **trader** | Polymarket 監控 | `*/30 * * * *` | 交易狀態、風險提醒 |

## 工作流程

### 常態運行
```
1. Cron 觸發 Isolated Session
2. Agent 執行任務
3. 寫入狀態到 memory/*.md
4. 等待下次觸發
```

### 用戶查看進度
```
用戶：「知識庫更新了嗎？」
→ Main Agent 讀取 memory
→ 「上次更新 2 小時前，新增 3 部影片」
```

### 異常通知
```
事件：Trader 發現虧損 > 3%
→ Isolated Session 發送通知
→ 你會收到 Telegram 訊息
```

## Memory 狀態檔案

| 檔案 | 內容 |
|------|------|
| `memory/knowledge-base.md` | 知識庫更新狀態 |
| `memory/content-schedule.md` | 內容發布排程 |
| `memory/trading-status.md` | 交易持倉狀態 |
| `memory/project-status.md` | 專案進度 |

## 設計原則

1. **每階段寫入 Memory** - 不靠記憶，寫入檔案
2. **重大異常通知** - 虧損、風險、市場劇變
3. **常態不打擾** - 正常運行不發通知
4. **獨立隔離** - Isolated Session 不會互相干擾

## 使用方式

### 查看所有 Cron 任務
```bash
openclaw cron list
```

### 手動觸發任務
```bash
openclaw cron run <job-id>
```

### 查看任務歷史
```bash
openclaw cron runs --id <job-id>
```

## 優勢

| 傳統方式 | Hybrid 模式 |
|---------|-------------|
| /new 後進度丟失 | Isolated 持續運行 |
| 每次都要重新執行 | 自動定期執行 |
| 不知道進度 | 可隨時查詢 memory |
| 中斷就停 | 獨立背景執行 |

## 風險控制

- **最大虧損**：Trader 單日不超過 5%
- **止損提醒**：虧損 > 3% 立即通知
- **人工確認**：重大決策必須用戶確認
